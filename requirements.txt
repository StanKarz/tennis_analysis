# Direct dependencies only. The previous version of this file was a 288-line `pip freeze`
# of a conda environment: 110 of those lines were `@ file:///...` local build paths that
# do not resolve on any other machine, so `pip install -r requirements.txt` failed
# immediately for anyone cloning the repo. It also omitted ultralytics entirely.
#
# Versions below are the ones recorded in that freeze, except ultralytics, which was
# missing from it — the pin here is the release current at the time of the last commit
# and is the one thing in this file that has not been verified against the original env.

torch==2.0.1
torchvision==0.15.2
ultralytics==8.1.24
opencv-python==4.8.1.78
numpy==1.26.2
pandas==2.2.0
scipy==1.11.4
matplotlib==3.8.3
